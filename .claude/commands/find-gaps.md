---
description: Find frameworks listed in IP-REGISTRY.md that don't have their own document yet
---

Use the `frameworks-methodology-agent` subagent to find documentation gaps in the framework portfolio.

For each framework in IP-REGISTRY.md, check whether a corresponding file exists in `frameworks/` or `frameworks/frameworks/`. List the missing ones in plain English.

Then check the reverse: any framework file with no IP-REGISTRY.md entry.

Under 250 words.
