---
description: Run a full Sovereign Leadership Architecture diagnostic — Silent Collapse markers, Five Imposters, Six Drives — and produce a reading.
argument-hint: "[optional: paste a situation, journal entry, or context — otherwise the agent will ask]"
---

You are running the full SLA diagnostic. Delegate to the `sla-diagnostician` subagent.

The user may have already provided context in the arguments: `$ARGUMENTS`

If `$ARGUMENTS` contains substantive context (a situation, journal entry, or self-description), pass it to the subagent as the starting material and instruct it to begin the diagnostic from that material — not from a blank slate.

If `$ARGUMENTS` is empty or insubstantial, instruct the subagent to open the diagnostic by asking the user for their starting context.

The subagent owns the full diagnostic flow:
1. Silent Collapse — the seven markers
2. The Five Imposters — which interventions has the user already invested in
3. The Six Drives — which drives are fractured, in what sequence
4. Synthesis — a reading in SLA voice

Do not run the diagnostic yourself. Delegate.
